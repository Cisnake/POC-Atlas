apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: gitlab-config-claim
   namespace: atlas-udd
spec:
   accessModes:
      - ReadWriteMany
   storageClassName: manual 
   resources:
      requests:
         storage: 3Gi 
